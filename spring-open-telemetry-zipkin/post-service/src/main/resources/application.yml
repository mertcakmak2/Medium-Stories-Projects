management:
  endpoints:
    web:
      exposure:
        include: health

  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces

  tracing:
    sampling:
      probability: 1.0

spring:
  application:
    name: post-service

server:
  port: 8080